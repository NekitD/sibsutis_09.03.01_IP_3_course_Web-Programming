

Программа:

int СокетКлиента = СоздатьСокет()
sockaddr_in АдресКлиента
АдресКлиента.СемействоАдресов = AF_INET
АдресКлиента.ИмяУстройства = htonl(INADDR_ANY)
АдресКлиента.Порт = 0

Инициализировать(СокетКлиента)

string Имя
cin >> Имя

string ИмяУстройства
int Порт
cout << "Введите имя ИмяУстройства"
cin >> ИмяУстройства
cout << endl;
cout << "Введите порт"
cin >> Порт

sockaddr_in АдресСервера
АдресСервера.СемействоАдресов = AF_INET
АдресСервера.ИмяУстройства = ИмяУстройства
АдресСервера.Порт = Порт

Соединение(СокетКлиента, АдресСервера)
Отправить(Имя)

Сообщение* Соб 

Вечный цикл:
    Получить(СокетКлиента, АдресСервера, Соб)
    cout << Соб.Основное << endl;
    int статус = Соб.Статус 

    Если (Статус == ВОПРОС_ГОТОВЫ_ЛИ_НАЧАТЬ_ИГРУ) Тогда:
        char Готов = ''
        cout << "Введите 'y' если готовы начать игру" << endl
        Если Готов == 'y' или Готов == 'Y' Тогда: 
            Отправить(СокетКлиента, АдресСервера, Готов)
        КонецЕсли
    КонецЕсли;

    Если (Статус == ВОПРОС_ГОТОВЫ_ЛИ_ВЫСТУПАТЬ) Тогда:
        char Готов = ''
        cout << "Введите 'y' когда будете готовы начать выступление" << endl
        Пока Готов != 'y' и Готов != 'Y':
            cin >> Готов
        КонецЦикла
    КонецЕсли

    Если (Статус == ВОПРОС_ЕСТЬ_ЛИ_У_ВАС_ВОПРОСЫ) Тогда:
        char Ответ
        cout << "Есть ли у Вас вопросы (Y/n)"
        cin >> Ответ
        Если ((Ответ == 'Y') и (Ответ == 'y')) Тогда: 
            string Вопрос
            cout << "Введите Ваш вопрос:" << endl;
            cin >> Вопрос 
            Отправить(СокетКлиента, АдресСервера, Вопрос)
        Иначе:
            Отправить(СокетКлиента, АдресСервера, "n")
        КонецЕсли
    КонецЕсли

    Если (Статус == ВОПРОС_ПОСТАВЬТЕ_ОЦЕНКУ) Тогда:
        int Оценка = -1
        Пока (Оценка < 1) или (Оценка > 5):
            cout << Поставьте оценку (1-5) << endl;
            cin >> Оценка
        КонецЦикла
        Отправить(СокетКлиента, АдресСервера, Оценка)
    КонецЕсли

    Если (Статус == ВОПРОС_ВЫБЕРИТЕ_СОИСКАТЕЛЯ) Тогда:
       vector<Профессия*> Профессии = Соб.Профессии
        Для П в Профессии:
            int Номер = -1
            vector<int> Соискатели
            Пока Номер не в Соискатели:
                cout << "Введите номер игрока, которого выбираете на вакансию" << П.Имя: << endl;
                cin >> Номер
            КонецЦикла
            Отправить(СокетКлиента, АдресСервера, Номер)
        КонецЦикла
        Отправить(СокетКлиента, АдресСервера, Оценка)
    КонецЕсли

КонецЦикла

Закрыть(СокетКлиента)
return 0;

Конец программы


